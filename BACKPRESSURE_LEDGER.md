# Backpressure Ledger: Personal Platforms

Canonical backpressure ledger for the four personal platforms (CanadaAccountants,
CanadaLawyers, CanadaInvesting, CanadaBusinessExits). Referenced from the
Backpressure Protocol in the global agent config (~/.claude/CLAUDE.md).

Append only. To retire a row, set Status to retired and give the reason in Notes.
Never delete a row.

Status values:
- pending: logged, mechanism NOT yet encoded (no code applied).
- active: mechanism encoded and enforcing.
- retired: was active, now removed; reason recorded in Notes.

Scope names the repos where the defect can occur. There is no live shared module
across these repos, so a code-level constraint added to one does NOT propagate;
each repo in Scope must get its own copy. Never write "shared template" in Scope.

| ID | Date | Mistake observed twice | Class | Mechanism | Rung | Scope | Status | Notes |
|----|------|------------------------|-------|-----------|------|-------|--------|-------|
| BP-001 | 2026-06-06 | Bounced addresses written through the unsubscribe path, corrupting unsubscribe/deliverability metrics | invariant | Reason-enum CHECK or FK guarding the bounce/unsubscribe write path. Tables are already separate, so do NOT specify a table split [SUPERSEDED 2026-06-09; see RE-SPEC in Notes] | 1 unrepresentable | canadaaccountants-backend, canadalawyers-backend, canadainvesting-backend (schema.sql); canadabusinessexits-backend as a NEW migration (CBE is migrations-only, no schema.sql) | pending | Ground truth 2026-06-06: ACC/LAW store unsubscribes as table `outreach_unsubscribes` and bounces as columns (`bounced`, `bounced_at`, `total_bounced`); INV has both in schema.sql plus `outreach_unsubscribes`; CBE migrations-only (`unsubscribe_tokens` table + `bounce_rate`/`bounced_count`). Tables already separate, so the leftmost mechanism is a write-path reason-enum CHECK/FK, not a table split. BEFORE marking active: confirm whether the wrong-write still recurs in the write paths; if they are already clean, reclassify as "regression insurance," not a live fix. No code applied. RE-SPEC 2026-06-09 (from the BP-001 send-path audit; see OPERATIONS.md): the original premise above is DISPROVEN. Bounces are NOT written through the unsubscribe path anywhere: ACC/LAW/INV track bounces as `outreach_emails.status='bounced'` (separate from `outreach_unsubscribes`); CBE uses a dedicated `bounces` table plus an `is_suppressed` flag. There is no bounce-as-unsubscribe conflation to guard. Actual audit findings: (1) unsubscribe suppression IS enforced at send time everywhere (ACC/LAW/INV via ~18 inline `NOT IN outreach_unsubscribes` clauses across send/selection queries; CBE via `is_suppressed=false` in `services/send-loop.js`, asserted again in `services/preflight-check.js`). (2) ACC/LAW/INV do NOT exclude prior hard-bounced addresses at enrollment (`scripts/enroll-cohort.js` excludes unsubscribes, bad-ZB, claimed, and dups, but not `status='bounced'`), so a hard-bounced address can be re-enrolled in a new sequence: a cross-campaign deliverability gap. CBE has no such gap (a bounce sets `is_suppressed=true`, and the send loop filters `is_suppressed=false`). (3) ACC/LAW/INV enforce unsubscribes by fragile inline repetition; one query that omits the clause silently mails suppressed people. Re-spec'd mechanism: adopt CBE's model on ACC/LAW/INV: a single suppression set (covering unsubscribes AND hard-bounces) checked at one send chokepoint, or a DB-level guard, replacing the inline-repeat clauses and adding bounce exclusion. Re-spec'd scope: canadaaccountants-backend, canadalawyers-backend, canadainvesting-backend are the targets; canadabusinessexits-backend is the REFERENCE model, not a target. Status stays pending (no code applied). Gated half: confirming the bounce gap actually recurs in data needs production DB read access (Phase 0.1 of the remediation plan). DATA RESULT 2026-06-09 (Step 2/3, read-only connects to the confirmed production instances): the 2026-06-09 RE-SPEC above was itself WRONG on its central claim. Production data shows bounces ARE written into outreach_unsubscribes: ACC 201/201, LAW 276/276, INV 307/307 bounced addresses are present in outreach_unsubscribes (100%), which carries a `reason` column. So the ORIGINAL BP-001 premise (bounces recorded in the unsubscribe/suppression table) is CONFIRMED at the data level for ACC/LAW/INV; the re-spec's claim that "bounces are NOT written through the unsubscribe path" was a code-grep artifact (grep saw status='bounced' on outreach_emails and missed that the bounce handler ALSO inserts into outreach_unsubscribes). NET EFFECT is two-sided: (a) PROTECTIVE for deliverability - because bounced addresses sit in outreach_unsubscribes, the NOT-IN-unsubscribes send filter suppresses them; re-sent-after-bounce is ACC 0/201, INV 3/307, LAW 9/276 (near-zero; the code-level enrollment gap barely fires). (b) METRICS conflation - outreach_unsubscribes mixes bounces and true unsubscribes (distinguished only by `reason`); counting rows as "unsubscribes" overcounts (about 16% of ACC's 1,255 are bounces). CBE is the clean reference (dedicated bounces + unsubscribes tables + is_suppressed; no conflation). Corrected mechanism: the reason-tagged suppression table WORKS for deliverability; the real fix is to ensure unsubscribe/deliverability METRICS filter by `reason`, with CBE's separate-tables model as the cleaner target. LESSON: code-grep misled this analysis TWICE (the original assumption, then the re-spec); only production data settled it, reinforcing candidate #13 (map by data/identity, never grep). |
| BP-002 | 2026-06-06 | Sentry.init called after the express require, missing setup-time errors | invariant | ESLint rule (sentry-before-express), leftmost form; applied per repo (no shared eslint config exists) | 2 lint | canadaaccountants-backend, canadalawyers-backend, canadainvesting-backend (CBE exempt) | active | Ground truth 2026-06-06: real in ACC/LAW/INV (server.js requires `@sentry/node` at L1, `express` at L2, but `Sentry.init` runs at L23-42, after the express require). CBE already satisfied via `instrument.js` (Sentry init-first in a separately pre-required file), so CBE is exempt. First encoding = BP-002 on canadaaccountants-backend as a separate proof step. Longer-term option: migrate ACC/LAW/INV to CBE's instrument.js pattern (structural fix rather than lint-enforced). ENCODED 2026-06-06 on canadaaccountants-backend (branch bp-002-sentry-before-express): ESLint 9 flat config (eslint.config.js) with the local rule eslint-rules/sentry-before-express.js registered inline as plugin `local`, scoped to server.js, error severity. Proof sequence: lint FAILED on server.js as-is (initAfterExpress at 41:3), then server.js reordered so Sentry.init runs before the express require; lint now PASSES (exit 0), node --check OK, npm test 2/2, and a controlled-load harness confirmed Sentry.init still receives SENTRY_DSN and runs before express loads. Live on ACC only; LAW and INV still pending (rule not yet applied there). |
| BP-003 | 2026-06-06 | Investing Postgres service link dropped after deploy | invariant | deploy-all.sh enforces it: `set -e` plus automatic Investing re-link after deploy | 4 build check | canadainvesting-backend, canadalawyers-backend, canadaaccountants-backend (the 3 backends deploy-all.sh covers) | active | Ground truth 2026-06-06: mechanism already LIVE in `~/deploy-all.sh` (`set -e` at L5 + "re-link Investing to canadainvesting-backend" step at end). OPEN hardening action: the canonical script lives loose in `~/` (in no repo); put it under version control so the stale copy at `~/Downloads/platforms/shared/deploy-all.sh` cannot be mistaken for canonical. OPEN: CanadaBusinessExits has a SEPARATE deploy path not covered by deploy-all.sh; verify how CBE deploys and whether it needs the same re-link safety. |
| BP-004 | 2026-06-09 | Production DB connection string hardcoded as a raw postgresql:// literal (embedded password) in the backend env, instead of a Railway service reference | invariant (candidate) | Use a Railway service-reference variable (e.g. a `${{Postgres.DATABASE_URL}}` reference) or a secret store; never a hardcoded literal with an embedded credential | policy (candidate) | canadaaccountants-backend (fulfilling-empathy) CONFIRMED; INV/CBE/LAW SUSPECTED (their DATABASE_URL fingerprints do not match their project Postgres, consistent with hardcoded literals) - confirm when their strings are inspected | pending (candidate) | Discovered 2026-06-09 during Step 1 identity resolution. The ACC backend's DATABASE_URL is a raw `postgresql://` literal pointing at `postgres.railway.internal` with credential pwfp 28d41e (user postgres) - a DIFFERENT role/password than the fulfilling-empathy Postgres service's managed credential (dd9a08), both on the SAME instance (system_identifier 7533606245792546852, confirmed by read-only connect). Leak surface: the production credential sits in plaintext env and will NOT rotate with the managed Postgres service. Relates to DB_MAP.md Flag A and the 2026-06-07 pino-http key-leak lesson. SURFACE-ONLY per operator standing guardrail: do NOT repoint or fix; map and understand only. |
