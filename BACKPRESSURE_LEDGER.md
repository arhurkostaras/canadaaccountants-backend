# Backpressure Ledger: Personal Platforms

Canonical backpressure ledger for the four personal platforms (CanadaAccountants,
CanadaLawyers, CanadaInvesting, CanadaBusinessExits). Referenced from the
Backpressure Protocol in the global agent config (~/.claude/CLAUDE.md).

Append only. To retire a row, set Status to retired and give the reason in Notes.
Never delete a row.

Status values:
- pending: logged, mechanism NOT yet encoded (no code applied).
- active: mechanism encoded and enforcing.
- retired: was active, now removed; reason recorded in Notes.

Scope names the repos where the defect can occur. There is no live shared module
across these repos, so a code-level constraint added to one does NOT propagate;
each repo in Scope must get its own copy. Never write "shared template" in Scope.

| ID | Date | Mistake observed twice | Class | Mechanism | Rung | Scope | Status | Notes |
|----|------|------------------------|-------|-----------|------|-------|--------|-------|
| BP-001 | 2026-06-06 | Bounced addresses written through the unsubscribe path, corrupting unsubscribe/deliverability metrics | invariant | Reason-enum CHECK or FK guarding the bounce/unsubscribe write path. Tables are already separate, so do NOT specify a table split [SUPERSEDED 2026-06-09; see RE-SPEC in Notes] | 1 unrepresentable | canadaaccountants-backend, canadalawyers-backend, canadainvesting-backend (schema.sql); canadabusinessexits-backend as a NEW migration (CBE is migrations-only, no schema.sql) | pending | Ground truth 2026-06-06: ACC/LAW store unsubscribes as table `outreach_unsubscribes` and bounces as columns (`bounced`, `bounced_at`, `total_bounced`); INV has both in schema.sql plus `outreach_unsubscribes`; CBE migrations-only (`unsubscribe_tokens` table + `bounce_rate`/`bounced_count`). Tables already separate, so the leftmost mechanism is a write-path reason-enum CHECK/FK, not a table split. BEFORE marking active: confirm whether the wrong-write still recurs in the write paths; if they are already clean, reclassify as "regression insurance," not a live fix. No code applied. RE-SPEC 2026-06-09 (from the BP-001 send-path audit; see OPERATIONS.md): the original premise above is DISPROVEN. Bounces are NOT written through the unsubscribe path anywhere: ACC/LAW/INV track bounces as `outreach_emails.status='bounced'` (separate from `outreach_unsubscribes`); CBE uses a dedicated `bounces` table plus an `is_suppressed` flag. There is no bounce-as-unsubscribe conflation to guard. Actual audit findings: (1) unsubscribe suppression IS enforced at send time everywhere (ACC/LAW/INV via ~18 inline `NOT IN outreach_unsubscribes` clauses across send/selection queries; CBE via `is_suppressed=false` in `services/send-loop.js`, asserted again in `services/preflight-check.js`). (2) ACC/LAW/INV do NOT exclude prior hard-bounced addresses at enrollment (`scripts/enroll-cohort.js` excludes unsubscribes, bad-ZB, claimed, and dups, but not `status='bounced'`), so a hard-bounced address can be re-enrolled in a new sequence: a cross-campaign deliverability gap. CBE has no such gap (a bounce sets `is_suppressed=true`, and the send loop filters `is_suppressed=false`). (3) ACC/LAW/INV enforce unsubscribes by fragile inline repetition; one query that omits the clause silently mails suppressed people. Re-spec'd mechanism: adopt CBE's model on ACC/LAW/INV: a single suppression set (covering unsubscribes AND hard-bounces) checked at one send chokepoint, or a DB-level guard, replacing the inline-repeat clauses and adding bounce exclusion. Re-spec'd scope: canadaaccountants-backend, canadalawyers-backend, canadainvesting-backend are the targets; canadabusinessexits-backend is the REFERENCE model, not a target. Status stays pending (no code applied). Gated half: confirming the bounce gap actually recurs in data needs production DB read access (Phase 0.1 of the remediation plan). DATA RESULT 2026-06-09 (Step 2/3, read-only connects to the confirmed production instances): the 2026-06-09 RE-SPEC above was itself WRONG on its central claim. Production data shows bounces ARE written into outreach_unsubscribes: ACC 201/201, LAW 276/276, INV 307/307 bounced addresses are present in outreach_unsubscribes (100%), which carries a `reason` column. So the ORIGINAL BP-001 premise (bounces recorded in the unsubscribe/suppression table) is CONFIRMED at the data level for ACC/LAW/INV; the re-spec's claim that "bounces are NOT written through the unsubscribe path" was a code-grep artifact (grep saw status='bounced' on outreach_emails and missed that the bounce handler ALSO inserts into outreach_unsubscribes). NET EFFECT is two-sided: (a) PROTECTIVE for deliverability - because bounced addresses sit in outreach_unsubscribes, the NOT-IN-unsubscribes send filter suppresses them; re-sent-after-bounce is ACC 0/201, INV 3/307, LAW 9/276 (near-zero; the code-level enrollment gap barely fires). (b) METRICS conflation - outreach_unsubscribes mixes bounces and true unsubscribes (distinguished only by `reason`); counting rows as "unsubscribes" overcounts (about 16% of ACC's 1,255 are bounces). CBE is the clean reference (dedicated bounces + unsubscribes tables + is_suppressed; no conflation). Corrected mechanism: the reason-tagged suppression table WORKS for deliverability; the real fix is to ensure unsubscribe/deliverability METRICS filter by `reason`, with CBE's separate-tables model as the cleaner target. LESSON: code-grep misled this analysis TWICE (the original assumption, then the re-spec); only production data settled it, reinforcing candidate #13 (map by data/identity, never grep). |
| BP-002 | 2026-06-06 | Sentry.init called after the express require, missing setup-time errors | invariant | ESLint rule (sentry-before-express), leftmost form; applied per repo (no shared eslint config exists) | 2 lint | canadaaccountants-backend, canadalawyers-backend, canadainvesting-backend (CBE exempt) | active | Ground truth 2026-06-06: real in ACC/LAW/INV (server.js requires `@sentry/node` at L1, `express` at L2, but `Sentry.init` runs at L23-42, after the express require). CBE already satisfied via `instrument.js` (Sentry init-first in a separately pre-required file), so CBE is exempt. First encoding = BP-002 on canadaaccountants-backend as a separate proof step. Longer-term option: migrate ACC/LAW/INV to CBE's instrument.js pattern (structural fix rather than lint-enforced). ENCODED 2026-06-06 on canadaaccountants-backend (branch bp-002-sentry-before-express): ESLint 9 flat config (eslint.config.js) with the local rule eslint-rules/sentry-before-express.js registered inline as plugin `local`, scoped to server.js, error severity. Proof sequence: lint FAILED on server.js as-is (initAfterExpress at 41:3), then server.js reordered so Sentry.init runs before the express require; lint now PASSES (exit 0), node --check OK, npm test 2/2, and a controlled-load harness confirmed Sentry.init still receives SENTRY_DSN and runs before express loads. Live on ACC only; LAW and INV still pending (rule not yet applied there). |
| BP-003 | 2026-06-06 | Investing Postgres service link dropped after deploy | invariant | deploy-all.sh enforces it: `set -e` plus automatic Investing re-link after deploy | 4 build check | canadainvesting-backend, canadalawyers-backend, canadaaccountants-backend (the 3 backends deploy-all.sh covers) | active | Ground truth 2026-06-06: mechanism already LIVE in `~/deploy-all.sh` (`set -e` at L5 + "re-link Investing to canadainvesting-backend" step at end). OPEN hardening action: the canonical script lives loose in `~/` (in no repo); put it under version control so the stale copy at `~/Downloads/platforms/shared/deploy-all.sh` cannot be mistaken for canonical. OPEN: CanadaBusinessExits has a SEPARATE deploy path not covered by deploy-all.sh; verify how CBE deploys and whether it needs the same re-link safety. |
| BP-004 | 2026-06-09 | Production DB connection string hardcoded as a raw postgresql:// literal (embedded password) in the backend env, instead of a Railway service reference | invariant (candidate) | Use a Railway service-reference variable (e.g. a `${{Postgres.DATABASE_URL}}` reference) or a secret store; never a hardcoded literal with an embedded credential | policy (candidate) | canadaaccountants-backend (fulfilling-empathy) CONFIRMED; INV/CBE/LAW SUSPECTED (their DATABASE_URL fingerprints do not match their project Postgres, consistent with hardcoded literals) - confirm when their strings are inspected | pending (candidate) | Discovered 2026-06-09 during Step 1 identity resolution. The ACC backend's DATABASE_URL is a raw `postgresql://` literal pointing at `postgres.railway.internal` with credential pwfp 28d41e (user postgres) - a DIFFERENT role/password than the fulfilling-empathy Postgres service's managed credential (dd9a08), both on the SAME instance (system_identifier 7533606245792546852, confirmed by read-only connect). Leak surface: the production credential sits in plaintext env and will NOT rotate with the managed Postgres service. Relates to DB_MAP.md Flag A and the 2026-06-07 pino-http key-leak lesson. SURFACE-ONLY per operator standing guardrail: do NOT repoint or fix; map and understand only. |
| BP-005 | 2026-06-12 | Directory pages rendered raw `generated_bio` (markdown `#`/`**` + wrong "Chartered Management/General Accountant" wording) because the `bio_snippet` read path bypassed `cleanBio`; a full-regen attempt then hammered the live API into HTTP 429, truncating/skipping pages | invariant | (1) `cleanBio` chokepoint on `bio_snippet` at the 3 `/api/directory/*` endpoints (the single point every consumer reads through) - ENCODED. (2) `generate-directory-pages.js` must throttle requests before any full regen (avoid 429) - NOT yet encoded. | 5 runtime guard (chokepoint, active) / 4 build check (throttle, pending) | canadaaccountants-backend (server.js directory endpoints) + `~/projects/generate-directory-pages.js` (loose generator, covers ACC/LAW/INV) | active (chokepoint) / active (throttle, 2026-07-05) | 2026-06-12 Scope C. Root cause: `/api/directory/{city,:province,:province/:designation}` served `bio_snippet` as `LEFT(generated_bio,160)` raw, so the directory generator embedded markdown + the CMA/CGA-as-"Chartered" wording onto 24 of 158 designation pages (profile pages were clean because their render applies cleanBio). FIX (active): `withCleanSnippet()` runs cleanBio then truncates, at all 3 endpoints - backend commit 4923fbb; raw `generated_bio` destructured out (no shape change); every future consumer inherits the correction. The 24 already-deployed static pages were surgically corrected (frontend commit 9ddb4994, 484+/484- balanced, in-bio-text only). DEFERRED, NOT done here: a full directory data-refresh (regen all 158) is a SEPARATE deliberate decision - a naive full regen (a) bundles unavoidable data-drift since the live pages predate current data (a 99-file / ~1981-deletion diff was observed and reverted), and (b) trips HTTP 429 on the live API. BEFORE any full regen: add request throttling to generate-directory-pages.js (the pending half of this row). The chokepoint means a future throttled regen is correct by construction. THROTTLE ENCODED 2026-07-05: all requests flow through apiFetch with a 400ms global minimum interval, 429-aware backoff (Retry-After honored, exponential fallback, 5-hit cap), and a fail-loud end-of-run gate (assertCompleteRun) that registers every final fetch failure and exits non-zero with a do-not-deploy message, so a partial estate can no longer masquerade as complete (the silent-truncation half of the original incident). Syntax-checked; live-run proof pends the next real regen (the 3.1 data-cleaning pass is the natural occasion). Stored 3,282 `generated_bio` rows left dirty BY DESIGN (corrected on read; claimed pros retire their own bios via the claim funnel, same retirement path as the oversell residual). |
| BP-006 | 2026-07-03 | Raw color hex in page CSS drifted from the design system repeatedly (pre-rebrand estate was 100+ distinct raw values; during the Phronisi pilot the same class of fix - map a raw hex to its token - was applied by hand across dozens of files and again for the generated directory estate) | invariant | CI gate `phronisi-gates.yml` on the ACC frontend: (1) hex-ban grep fails any push/PR with raw color hex outside `assets/phronisi-tokens.css` (exempt: entities, theme-color meta, pure #fff/#000, /public legacy, email JS which cannot resolve var()); failure message names each offending file:line and points to the PR #3 mapping table. (2) Lighthouse accessibility >= 90 on the six key pages. | 4 build check | canadaaccountants, canadalawyers, canadainvesting (each frontend carries its own phronisi-gates.yml copy; no shared workflow layer exists). | active | Encoded on branch phronisi-directory-regen (rode the step-5 regen PR #4, merged 2026-07-03). Generator templates retokened in the same pass so regens emit tokens natively: tools/tier1-pregen/gen-ssr.js (backend, this repo) and ~/projects/generate-directory-pages.js (loose script, BP-005's subject). Local dry-run of the grep across root + all 6,479 generated pages: 0 violations. ACTIVE 2026-07-03: PR #4 merged; both gates ran green on the PR and again on the main push (hex-ban pass, lighthouse-a11y pass). SCOPE COMPLETE 2026-07-03: LAW (canadalawyers#4) and INV (canadainvesting#2) merged with their own workflow copies; gates ran green on both PRs, both main pushes, and on the LAW homepage nav one-liner pushed after the live sweep. generate-directory-pages.js tokensReady flags now true for ACC/LAW/INV, so the guard only remains armed for future platforms (CBE). Incident note for the record: the LAW platform agent's step-5 regen pulled live-API data drift into 18 of 28 listing pages (BP-005 failure mode) and reported it as shell-only; caught by independent diff review in the main session, restored from main, re-transformed in place (canadalawyers f2fb08f). Lesson reinforces BP-001/BP-005: verify agent claims against data (diffs), never against reports. |
| BP-007 | 2026-07-06 | Backend deployed from a stale or side-branch base, silently regressing already-shipped work (twice in one session: LAW `railway up` from a June-10-era local main overwrote the July 2 paid-onboarding + whitelist-v2 code for ~22h; INV deployed from the June-29 inv-boot-crash-resilience branch, dropping ux-15/ux-19 for ~24h). Cause: `railway up` ships the local working tree with no base check; local mains had drifted behind origin while other sessions merged PRs. | invariant | Pre-deploy base guard: a repo-local `scripts/safe-deploy.sh` that (1) `git fetch origin`, (2) refuses to deploy unless HEAD contains origin/main (`git merge-base --is-ancestor origin/main HEAD`), (3) prints the exact divergence when refusing. All manual deploys go through it; raw `railway up` retired from runbooks. | 4 build check | canadaaccountants-backend, canadalawyers-backend, canadainvesting-backend, canadabusinessexits-backend | active (3 of 4; CBE pending) | Detected 2026-07-06 during the verified-sweep staging when the INV sweep agent noted the province-contract test "missing from origin/main" - the loose thread that unraveled the divergence. Remediated same session: LAW main merged (guard + onboarding coexist, e381c80) and redeployed; INV main reconciled (origin + OOM/province/engines side branch, 0908097) and redeployed; both cutovers verified healthy, INV province fix re-verified live (HTTP 200), self-test artifacts deleted (0 residue). ACC local main was also behind but never deployed stale - ff-synced. ENCODED 2026-07-08: `scripts/safe-deploy.sh` is live on origin/main of all three personal backends (ACC `1321c86`, LAW `e88a2f2`, INV `fc69276`) and was the deploy path for all three verified-sweep cutovers - its base-check printed "Base check passed: HEAD contains origin/main" on each, so the mechanism is proven in production, not just present. Raw `railway up` retired from the deploy runbooks for these three. **CBE (canadabusinessexits-backend) still pending**: the guard is NOT yet in that repo (verified 2026-07-08: 0 occurrences on CBE origin/main); it gets its own copy when its next backend change lands. Row stays open until CBE is covered. |
| BP-008 | 2026-07-08 | Member-facing copy promised a paid perk (client match notifications) that code delivered to everyone, and the paid perk itself reached free tier in production (LAW friction notify live with no tier gate; the /api/match-lawyers leg had no flag, no self-test guard, and no gate at all). Same class twice: the CRM day-10 table and the claim-flow emails contradicted each other on both platforms. | invariant | Paid-perk tier gate in code: every LAW notify leg checks lawyer_subscriptions (status='active' AND tier NOT IN ('free')) before sending, fail-closed, loud skip log naming the ruling; /api/match-lawyers gained flag + self-test guard + gate; ClientSignal cron gated while dark. Copy and code landed in the SAME deploy (e708fb0) so promise and behavior cannot diverge in either direction. | 5 runtime guard | canadalawyers-backend (live); ACC equivalent parked with the flag-flip conditions (notify legs flag-off + moratorium there) | active | Verified by read-only production simulation post-deploy: LAW pro id 2 (professional) NOTIFY-eligible; claimed free-tier (scraped id 130743) SKIP on both predicates. Fail-closed is structural (exactly 1 paid sub row exists). Ruled 2026-07-08 (R-J: rule for the table). |
| BP-009 | 2026-07-08 | Quantified, code-unsupported claims kept surviving copy passes by changing surface: "guaranteed top 3" escaped the 2026-07-05 pass on pricing.html, its siblings hid on index.html and inside the revenue calculator, cap claims dodged the ban string by dropping "per month", and a stale raw count (222,000+) lived on in backend email templates the frontend gates never see. | invariant | Three mechanisms: (1) copy-ban CI gate extended with the #45 family + funnel-analytics + suffix-proof cap regex ("[Uu]p to N (active )?(client )?matches"), zero exclusions; (2) tests/count-claims.test.js on LAW bans the 22x,000+ raw-count family in backend templates with a failure message naming the honest source (whitelist 140,000+, B18) - it caught a second stale count pre-merge; (3) the INV revenue calculator retired outright (ruling R-K branch c: failed #22 on all three prongs; DB truth 0 matches all-time), redirect stub + 131 links removed + generator source patched so regens cannot re-emit it. | 2 lint or static check | canadainvesting (gate, live); canadalawyers-backend (count test, live); count-test pattern should propagate to ACC/INV backends per Step 5; sibling calculators on ACC and LAW are HELD for Arthur's ruling and are listed in the parked queue | active | The calculator lesson generalizes: a calculator on a pricing page is a claim in math's clothing and is held to the same standard as a sentence. |
| BP-010 | 2026-09-07 | The served ACC profile sitemap drifted from the pages the host actually serves, twice: the pre-June sitemap-profiles-1/2.xml was a raw id-range dump (~12,800 `?id=` URLs, <1% qualifying) while the backend generator route kept its own looser email-only filter and the legacy `?id=` URL form; and after the June-11 static corpus the sitemap was hand-assembled from a generator run so a later DB change (199 rows lost city/firm, 1,800 gained email+bio) left the live sitemap listing 199 below-threshold pages and missing 1,800 qualifying ones with no signal. Root cause: four independent copies of "which profiles are public" (API gate, sitemap route, page generator, directory listings) and two artifacts (pages, sitemap) written by different steps. | invariant | (1) ONE predicate: `utils/profile-indexability.js` exports INDEXABLE_SQL + classifyProfile(); the profile API (`indexable` flag, related links), the sitemap route, the directory listings (gated rows filtered, `profile_url` only when public) and `tools/tier1-pregen/gen-db.js` all read it; `tests/profile-indexability.test.js` pins the threshold in both forms and greps server.js for the wiring. (2) ONE writer: gen-db.js writes pages, prunes, and the sitemap (+ sitemap_index.xml, robots.txt) in a single run, dry-run by default. (3) Frontend CI gate `scripts/check-sitemap-profiles.mjs` (phronisi-gates `sitemap-parity` job) fails any push/PR where sitemap-profiles-N.xml and /profile/{id}/index.html are not 1:1, or a url is not the static form, or index/robots are unwired; failure names counts, sample ids and the regen command. (4) `GET /api/admin/profile-index-drift` reads live sitemap vs threshold between regens. | 4 build check (gate) + 3 unit test (predicate) + 5 runtime (drift read) | canadaaccountants (gate + generated artifacts); canadaaccountants-backend (predicate, generator, API, test). LAW/INV have no static profile corpus yet; when the LAW prerender lands it gets its own copy of (1)-(3). | active on merge (encoded on branches profile-index-status, backend + frontend, 2026-09-07; PRs held) | GitHub Pages cannot emit 410, so a gated profile's page url is a real 404 (file absent) rather than 410; the SPA `?id=` form carries robots noindex on API 404/410/indexable:false. 410 at the page url needs an origin with status control (Cloudflare proxy or Firebase rewrite = DNS change, Tier 2, Arthur's call). Threshold count 2026-09-07: 7,911 indexable of 101,935 rows (1,229 gated, 91,727 no email). Claimed pages are never rewritten or pruned by the generator without --include-claimed. |
| BP-011 | 2026-09-07 | `railway up` run from a git worktree archived the PARENT checkout (the "project directory" the Railway link was made in), not the worktree, so four canadalawyers-backend deploys from two worktrees (two sessions, same hour) reported SUCCESS while shipping the main checkout's branch (2ee5972); every route in the deploying branches stayed 404. The BP-007 base check passed each time because it inspects the worktree's HEAD, which was not the tree uploaded. | invariant | `scripts/safe-deploy.sh` now runs `railway up --path-as-root --service $SERVICE --detach "$(git rev-parse --show-toplevel)"` and prints the tree path + short SHA it uploads, so the tree the base check approved is the tree Railway receives. | 4 build check | canadaaccountants-backend, canadalawyers-backend, canadainvesting-backend (each repo carries its own copy; landed on the profile-disputes branches, PRs ACC #23 / LAW #13 / INV #12); CBE still has no safe-deploy.sh (BP-007 row) | pending (merge of the three PRs) | Found 2026-09-07 by the lead-notify session after its deploy be18f93e came up without its routes; confirmed against my own deploys 4861fc9a and 6081200c the same way. Lesson added to the verification rule: a deployment status of SUCCESS and a passing base check are not proof; hit a route or read a boot-log line that only the new tree produces. Railway CLI 4.30.1 documents the flag as "Use the path argument as the prefix for the archive instead of the project directory". |
| BP-012 | 2026-09-07 | A claim-state write named a column or side effect that production does not have, and a non-fatal catch swallowed the error, so the state transition looked complete and was not. Twice on LAW the same day: (1) the Stripe webhook's application-payment branch wrote `scraped_lawyers.claimed_at = NOW()`; production has no such column (claim columns are claim_status, claimed_by, claim_token, claim_requested_at), so Postgres raised "column does not exist" into the catch and the matched profile was never marked claimed, never got claimed_by, and the admin backfill endpoint and dashboard joins that key on claimed_by could not see it (production: 2 claimed rows, both via /api/claim/instant, 0 status='converted'; the webhook path never once succeeded). (2) /api/claim/instant inserted lawyer_profiles with ON CONFLICT (user_id), a column with no unique index in production, so every instant claimant since 2026-05-18 got no profile row (LAW PR #15). Same class as the 2026-04-21 ACC incident in the state-transition rule: two paths write the same visible state, only one carries the side effects. | invariant | Source-assertion test `tests/scraped-lawyers-claimed-at.test.js` (node --test) scans every .js file outside node_modules and tests: (a) no SQL literal touching scraped_lawyers may reference claimed_at; (b) every `UPDATE scraped_lawyers SET ... claim_status = 'claimed'` must bind `claimed_by = $n`, with the writer count pinned so a new claim path has to opt in (pinned at 2 in PR #17, raised to 3 by the lead-notify merge for modules/leads/claim.js). Failure text names file:line, the expected shape, the Postgres error the catch was hiding, and the fix. The sibling `tests/lawyer-profiles-conflict-target.test.js` (PR #15) pins every lawyer_profiles ON CONFLICT target to the columns that are unique in production. | 3 unit test (source assertion) | canadalawyers-backend (both tests live on main, deployed d17337c 2026-09-07). canadainvesting-backend (fix + `tests/scraped-advisors-claimed-by.test.js` live on main, INV PR #13 merged 675f8c4, deployed 273fc1c9 2026-09-07 19:03Z; its webhook had written `UPDATE scraped_advisors SET claim_status = 'claimed' WHERE id = $1` with no claimed_by while /api/claim/instant set it, so the row was claimed in name and invisible to the three dashboard joins on sa.claimed_by). canadaaccountants-backend (`tests/scraped-cpas-claimed-by.test.js` live on main, ACC PR #32 merged 8b60071 2026-09-07; regression insurance only: ACC has one claim writer, the instant path, and it already sets claimed_by; production scraped_cpas read-only check the same day: claim_status, claimed_by integer, claim_token, claim_requested_at, no claimed_at, 2 claimed rows both with claimed_by; the writer count is pinned at 1 and the scanner floor is 40 because server.js alone carries 49 scraped_cpas literals; proof was a mutation copy of server.js with the LAW defect shape injected, which failed both assertions at server.js:6218). SECOND ACC OCCURRENCE of this class, found 2026-09-07 by the BP-014 live check: five statements named `cpa_subscriptions.plan_type` (webhook upsert, which also omitted the NOT NULL `email`; seo-score; pipeline MRR inside an empty catch; dashboard/matches) while production has `tier`; schema.sql declares plan_type. Encoded in ACC PR #39 as a third assertion in `tests/cpa-subscriptions-cpa-profile-id.test.js` (no cpa_subscriptions literal may name plan_type; flags server.js 119, 3002, 5396, 6556 on the pre-fix tree). | active (LAW, INV, ACC) | Fix shipped as LAW PR #17 (merge 8d84742): the users upsert moved ahead of the claim stamp so claimed_by binds the member's users.id, matching /api/claim/instant; founding_member left to the instant path on purpose. Leftmost rung would be the schema itself (the column is unrepresentable already; the defect is code naming it), so a source assertion is the leftmost rung that fires before deploy. The real end-to-end proof (a live checkout.session.completed with matched_lawyer_id, then claim_status='claimed' and claimed_by set on the row) has NOT run yet; this row records the guard, not that proof. Lesson: a production information_schema read is the only trustworthy column list; the boot-migration CREATE TABLE in server.js (which does declare claimed_at on signal_emails) is not it. AMENDED 2026-09-07 (second session, same defect found independently): read-only production check the same day showed 0 lawyer_applications with status='paid' and 0 carrying the webhook's Stripe note, so the broken branch had never FIRED on real data (not merely never succeeded); no backfill is owed. A parallel fix, LAW PR #16 (branch fix/stripe-webhook-claimed-at, left unmerged), extracted the application-payment chain into routes/stripe-application-payment.js with seven mocked-pool behavior tests (exact SET column list, users insert before the claim stamp, full chain incl. setup token and enrollment, a rejecting stamp logged without halting the chain) and per-leg isolation of the claim stamp; it conflicted with #17 after merge and was closed as superseded rather than deployed as a refactor of a live payment path without a Stripe end-to-end run. Those tests are the available upgrade from this row's rung-3 source assertion to a behavior test if wanted later. The general form of the guard (assert every UPDATE's SET columns against repo schema sources) was NOT encoded: repo schema sources do not match production (LAW `is_misclassified` / `misclassified_reason` are written at server.js ~7775 with no CREATE or ALTER anywhere in the repo), so it would need an unverified allowlist; reopen once a repo-tracked information_schema snapshot exists per platform. INV 2026-09-07: production scraped_advisors read-only check (claim_requested_at, claim_status, claim_token, claimed_by integer, founding_member, status; no claimed_at; users.email unique) confirmed the INV statement never crashed, it only left claimed_by NULL; 4 claimed rows in production, all via the instant path, none missing claimed_by, so no backfill owed there either. The INV fix mirrors #17 and adds one named throw in the profile block (advisor_profiles.user_id and email are nullable in INV production, so without the inline users insert a missing application row would have produced a NULL-user profile row instead of the old NOT NULL failure into the same catch). INV end-to-end proof (live checkout.session.completed with matched_advisor_id) has NOT run; same standing as LAW. INV DEPLOY VERIFICATION 2026-09-07: 273fc1c9 checked by behavior, not status: the PR #12 profile-dispute route answers 200 on production against a 404 control, so the live tree is post-merge; PR #13 adds no observable endpoint, so its presence in that upload is inferred from the deploy starting 33 s after the merge, not proven. |
| BP-013 | 2026-09-07 | INSERT ... ON CONFLICT (col) written against a column with no unique index in production; Postgres rejects every call and the surrounding non-fatal catch swallows it, so the write silently never happens. LAW `/api/claim/instant` shipped with `ON CONFLICT (user_id)` on lawyer_profiles (unique only on id, cpa_id, email, referral_code); every instant claimant since 2026-05-18 (user 24) got no profile row and was invisible to dashboard + matching. Recorded as occurrence (2) inside BP-012's description; split out here because the class is distinct: BP-012 is a write naming a column production lacks, BP-013 is a conflict target naming a uniqueness production lacks, and the guard for each is a different assertion. | invariant | `tests/lawyer-profiles-conflict-target.test.js` (canadalawyers-backend): scans every INSERT INTO lawyer_profiles in the repo and fails if an ON CONFLICT target is not a production-unique column; message states found / expected / cause / fix. UNIQUE_COLUMNS is the allow-list; a new unique index adds its column in the same PR. | 3 unit test (source assertion) | canadalawyers-backend (live on main, PR #15 merged e152cf4, deployed via autodeploy d17337c 2026-09-07); canadaaccountants-backend (`tests/cpa-profiles-conflict-target.test.js`; PR #34 merged 5e5cad1, deployed d7ffd3d8 2026-09-07 19:49Z; the scan found the SAME live defect: /api/claim/instant used ON CONFLICT (user_id) on cpa_profiles, unique only on id, cpa_id, email, referral_code per pg_indexes; read-only EXPLAIN against production errored on the old target and planned on cpa_profiles_email_key with the fix. A SECOND defect surfaced behind it: cpa_profiles.cpa_id is NOT NULL with no default and the insert never named it; fix + third assertion "every INSERT INTO cpa_profiles supplies cpa_id" = PR #38 merged 4705893, autodeploy ca5f146a 21:26Z. 1 of 2 claimants, user 7 / scraped_cpas 40831, has no profile row; that claim reached a wrongly enriched address (Lucille@WPFixIt.com on Nathan DeJonge's row), so it awaits Arthur's ruling, backfill vs unclaim, not a mechanical backfill); canadainvesting-backend (`tests/advisor-profiles-conflict-target.test.js`; PR #14 merged 4da0d8f, deployed 6926ac51 19:48Z; same (user_id) defect on advisor_profiles, unique only on id, advisor_id, email; same EXPLAIN proof on advisor_profiles_email_key. A THIRD defect surfaced when the approved one-row backfill, written in the insert's shape, aborted: the insert named `city`, which production advisor_profiles does not have; fix + third assertion pinning the 25 production columns = PR #16 merged ee07f82, autodeploy e316f223 21:26Z. Backfill for user 18 / scraped_advisors 16881 EXECUTED 21:22Z on Arthur's word, advisor_profiles id 7; 0 INV claimants now lack a profile row). All three platforms shipped the identical (user_id) target from the same copied handler, so the fix landed three times rather than once, and ACC and INV each hid one more schema-drift defect behind it that only a production write attempt exposed. Leftmost rung (a UNIQUE index on user_id) still deferred on all three because it is a production schema change needing its own approval | active (LAW, ACC, INV) | Proof: both tests fail on the previous server.js and pass on the fix; read-only EXPLAIN on LAW production showed the old statement erroring and the new one planning on lawyer_profiles_email_key. E2E on production 2026-09-07 18:38Z (Arthur's word): diagnostic claim via /api/claim/instant created lawyer_profiles row 10 for the new user, dashboard read returned it, admin notify landed in 1s, cleanup by exact id left 0 residue. One-row backfill for user 24 executed 18:51Z on Arthur's word (lawyer_profiles id 11); both LAW claimants now have a profile row. Lesson: a conflict target is a schema assumption; verify it against pg_indexes, not schema.sql (schema.sql and prod disagree on this table: lawyer_id vs cpa_id). |
| BP-014 | 2026-09-07 | A query compared `lawyer_subscriptions.cpa_profile_id` (VARCHAR(255) in production, one row holding '2') to `lawyer_profiles.id` (INTEGER) without a cast, so Postgres raised "operator does not exist: character varying = integer" at runtime. Same class as BP-012 (the boot CREATE TABLE declares the column INTEGER; production disagrees; a silent catch hid one instance). Three live sites on LAW main: POST /api/stripe/create-portal-session (billing portal, 500 for every member), GET /api/lawyer/seo-score (same LEFT JOIN), POST /api/admin/e2e-test-cleanup (subquery on lawyer_profiles.id inside `.catch(() => ({rowCount:0}))`, also ordered after the profile delete so it could never match); plus scripts/profile-removal.js. Found 2026-09-07 when a manual `DELETE ... WHERE cpa_profile_id = 10` failed the same way. Three other sites had already been patched one at a time with a one-sided `lp.id::text = ls.cpa_profile_id` (client-signal tier gate, modules/leads/notify.js, scripts/lead-selftest.js): the correction had been applied by hand more than twice. | invariant | (1) Source-assertion test `tests/lawyer-subscriptions-cpa-profile-id.test.js` (node --test) scans every .js file outside node_modules and tests and fails on any comparison of cpa_profile_id to lp.id, a bare lawyer_profiles id (including `IN (SELECT id FROM lawyer_profiles`), or an integer literal without a cast; failure names file:line, the Postgres error and the expected shape; a second test pins the detector on the exact production shapes and the accepted forms. (2) Convention: text compare on both sides (`ls.cpa_profile_id::text = lp.id::text`), which holds under either column type. (3) Leftmost rung, proposed not run: flag-gated boot migration `LAWYER_SUBS_CPA_PROFILE_ID_RETYPE=true` runs `ALTER TABLE lawyer_subscriptions ALTER COLUMN cpa_profile_id TYPE INTEGER USING cpa_profile_id::integer` after refusing on any non-integer value, making the mismatch unrepresentable; verified on a throwaway local DB (unique index and UPSERT survive). | 3 unit test (source assertion) now; 1 unrepresentable once the retype is approved and run | canadalawyers-backend (LAW PR #19, branch fix/lawyer-subscriptions-cpa-profile-id-type). canadaaccountants-backend (ACC PR #37, branch fix/cpa-subscriptions-cpa-profile-id-type: same casts on the four joins, webhook unique-index catch now logs, gated CPA_SUBS_CPA_PROFILE_ID_RETYPE migration, tests/cpa-subscriptions-cpa-profile-id.test.js) has the SAME defect, verified read-only 2026-09-07: `cpa_subscriptions.cpa_profile_id` is VARCHAR(255) (2 rows, both canceled, both NULL profile id; no unique index on the column, so the webhook's `ON CONFLICT (cpa_profile_id)` upsert at server.js ~L114 would also fail the BP-012 way unless its silent `CREATE UNIQUE INDEX ... .catch(() => {})` at L112 runs first) against `cpa_profiles.id` INTEGER, with four uncast joins: GET /api/cpa/seo-score (~L2976), GET /api/stripe/subscription-status (~L5577), POST /api/stripe/create-portal-session (~L5589), GET /api/dashboard/matches (~L6523).  canadainvesting-backend is CLEAN, verified the same way: `advisor_subscriptions.advisor_profile_id` is INTEGER (1 active row, profile 5, unique index uq_advisor_subscriptions_advisor_profile_id), so its uncast joins are correct and need no change. | active (LAW: PR #19 merged 1e819a4, autodeploy 1c6541ac, proven live; ACC: PR #37 merged b062d10, autodeploy 34f146ea, cast fix proven live, two endpoints still 500 on the plan_type drift until PR #39 merges; INV needs nothing). Retype migrations NOT run on either platform. | Renumbered from BP-013 on 2026-09-07 after a parallel session took BP-013 for the ON CONFLICT class. Production read 2026-09-07 with default_transaction_read_only=on: cpa_profile_id VARCHAR(255) NULLable, unique index idx_lawyer_subs_cpa_profile_id, 1 row, value '2', 0 non-numeric. Parameter comparisons (`= $1`) were never affected because an untyped parameter adopts the column type; only column-to-column joins, subqueries and literals break, which is why /api/stripe/subscription-status worked while the portal endpoint next to it did not. Merging PR #19 deploys casts only (LAW autodeploys main); setting the Railway variable is the dedicated approval for the schema change, after which a follow-up PR removes the flag block and the casts can go. LIVE PROOF 2026-09-07 20:30Z, real member tokens minted with each platform's JWT_SECRET, never printed: LAW (user 16, the one real subscriber) subscription-status 200 with the row, seo-score 200, create-portal-session 200 with a billing.stripe.com session URL. ACC (user 4, David Mark, no subscription row) subscription-status 200 null, create-portal-session 400 "No Stripe customer found" (correct), seo-score 500 and dashboard/matches 500 on "column cs.plan_type does not exist": a SECOND defect under the first, production cpa_subscriptions has `tier` and a NOT NULL `email`, never `plan_type`, while schema.sql declares plan_type; the Stripe webhook upsert named plan_type and omitted email, so it could never have written a row (both production rows have NULL profile id and were written elsewhere). ACC PR #39 renames the five sites to tier, binds email on the upsert, logs the MRR catch, and adds a third assertion to the test (no cpa_subscriptions literal may name plan_type); recorded under BP-012's class in its Scope. The click, not the green suite, found it: the same lesson as the shipping rule. |
